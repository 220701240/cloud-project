apiVersion: v1
kind: Service
metadata:
  name: internship-api-service
  namespace: placement-tracker
  labels:
    app: internship-api
spec:
  selector:
    app: internship-api
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP